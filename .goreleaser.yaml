version: 2

builds:
  - dir: openhands/wrapper
    main: .
    binary: strawpot_openhands
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w

archives:
  - formats: [binary]
    name_template: "strawpot_openhands-{{ .Os }}-{{ .Arch }}"

release:
  github:
    owner: strawpot
    name: strawpot_openhands_cli
